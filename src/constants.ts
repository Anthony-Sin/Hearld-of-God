import { Shield, Hash, Flame, Bird, User, Crown } from 'lucide-react';

export const SYMBOL_ICONS: Record<string, any> = {
  lion: Shield,
  eagle: Bird,
  dragon: Flame,
  shield: Shield,
  cross: Hash,
  star: Hash,
  sun: Flame,
  moon: Hash,
  bird: Bird,
  wolf: User,
  crown: Crown,
};

export const MAP_WIDTH = 160;
export const MAP_DEPTH = 160;
